// Macro MAX with args ['a', 'b'] expanding to '((a) > (b) ? (a) : (b))'

// Macro RETURN with args None expanding to '200'
int main()
{

    const int a = 0, b = 6;
    if (((a) > (b) ? (a) : (b)) == b )
        return 200;



  return 0;
}
